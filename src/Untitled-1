function a(){
  console.log('a')
}
function b(){
  console.log('b')
}

var funcs = [a,b];
const last = funcs[funcs.length - 1];
const rest = funcs.slice(0, -1);

console.log((...args) => rest.reduceRight((composed, f) => f(composed), last(...args)))